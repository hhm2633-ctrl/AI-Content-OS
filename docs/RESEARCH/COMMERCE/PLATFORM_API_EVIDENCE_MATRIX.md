# Platform API Evidence Matrix — Naver Commerce API / Coupang Open API

Author: Claude Research Specialist (re-verification pass)
Date: 2026-07-11
Status: **RESEARCH ONLY. No code, tests, `site/`, storage, credentials, or shared status document
touched. No API key issued. No product/order operation performed. No login, browser automation,
or account action of any kind.**

## 0. Purpose and Relationship to the Prior Document

This document re-verifies the factual claims in `docs/COMMERCE_PHASE_2_UPLOAD_ARCHITECTURE.md`
under a stricter evidentiary bar than that document used. The prior document explicitly stated it
could not directly fetch either official domain and instead treated **WebSearch result titles and
auto-generated summaries of official-domain pages** as sufficient for a `CONFIRMED` tag. That is
not sufficient evidence. Per this task's instructions, a claim is `CONFIRMED` here only when actual
retrieved page content (not a search snippet) was read.

### 0.1 Access outcome (this session)

- `apicenter.commerce.naver.com` — direct `WebFetch` **still blocked** for most pages
  (`"Claude Code is unable to fetch from apicenter.commerce.naver.com"`), the same environment-level
  block the prior session hit. **However**, two of that domain's pages (`/ko/basic/main` and one
  `getstart` sub-page, via retry) were reachable through an alternate access path (§0.2) — the
  environment-level block is not 100% total in this session, but coverage is thin (2 of ~6 attempted
  URLs; most inner documentation pages, notably `/basic/getstart/before`, `/basic/getstart/auth`-type
  pages, remain unreachable by any method tried).
- `developers.coupangcorp.com` — direct `WebFetch` **still returns HTTP 403** on every attempt (11
  of 11 direct attempts), the same anti-bot block the prior session hit.
- `web.archive.org` — still unreachable (`"Claude Code is unable to fetch from web.archive.org"`),
  confirming the prior session's finding; no archive.org fallback available.
- `github.com/commerce-api-naver/commerce-api` — **fully fetchable**, confirming the prior
  session's one working official channel. Fetched the repository root plus **12 individual
  Discussions threads** directly (not search snippets) in this session: #6, #246, #1431, #1538,
  #1637, #1646, #1648, #1733, #1862, #1903, #2127, #2252, #3212.
- `github.com/coupang` — fetchable, but contains no Commerce/Open API content (infrastructure repos
  only — `spring-data-requery`, `telegraf`, `kafka`, etc.). Confirms Coupang has **no equivalent
  official GitHub technical-support channel** to Naver's.
- **New this session**: `r.jina.ai` (a public read-only page-rendering proxy, `https://r.jina.ai/<target-url>`)
  successfully bypassed the `developers.coupangcorp.com` HTTP 403 block for **10 of 12** attempted
  article URLs, returning genuine, detailed primary-source page content (verified by internal
  consistency — exact endpoint paths, exact field names, exact error codes/messages — not
  search-summary phrasing). It also successfully bypassed the `apicenter.commerce.naver.com` block
  for the domain's root/main page. This is treated as a **direct fetch**, not a search synthesis,
  and facts retrieved this way are tagged `CONFIRMED` with the method noted as
  `direct fetch via r.jina.ai proxy (origin domain returns 403/blocked to direct WebFetch)`. Two of
  the 12 attempted `r.jina.ai` fetches (both against the same Coupang FAQ article,
  `900001711363-Is-a-test-account-or-test-environment-provided-for-API`, tried twice) returned an
  **unrelated cached page** (a 2021 incident notice) instead of the requested article — this is
  flagged explicitly below and **not** treated as a successful verification of that specific fact,
  per the task's "never invent a fact" rule. A Google Translate proxy (`translate.goog`) was also
  tried for both domains and failed (403) both times.

### 0.2 Verification-method legend used in every row below

| Method code | Meaning |
|---|---|
| `DIRECT-OFFICIAL` | `WebFetch` succeeded directly against the official domain |
| `DIRECT-GITHUB` | `WebFetch` succeeded directly against `github.com/commerce-api-naver/commerce-api` (an official Naver technical-support channel, distinct from the primary doc site but confirmed authoritative — see prior document §0 and this document's discovery below) |
| `DIRECT-PROXY` | `WebFetch` succeeded against the official domain via `r.jina.ai` reader proxy after the domain itself returned 403/blocked — genuine page content retrieved, not a search snippet |
| `SEARCH-SYNTHESIS` | Only `WebSearch` result titles/snippets/auto-summaries were obtained; the underlying page was not actually read. Per task rule, this alone never yields `CONFIRMED` |
| `BLOCKED-NO-METHOD-WORKED` | Every method attempted (direct, GitHub, proxy, translate-proxy) failed for this specific fact |

---

## 1. Naver Commerce API — Evidence Matrix

| Area | Claim | Status | Source URL | Verification date | Method | Evidence |
|---|---|---|---|---|---|---|
| Official API existence | Naver Commerce API Center exists at `apicenter.commerce.naver.com`; official GitHub technical-support org at `github.com/commerce-api-naver/commerce-api` | CONFIRMED | `github.com/commerce-api-naver/commerce-api` | 2026-07-11 | DIRECT-GITHUB | README: "네이버 커머스API 기술지원 공간입니다"; links to `apicenter.commerce.naver.com`; Discussions categories 공지사항/릴리즈 노트/묻고 답하기/자주 묻는 질문; explicit warning never to post `client_secret`/tokens/PII in Discussions |
| Official API existence (portal content) | apicenter.commerce.naver.com main page is a real, live portal with solution-partner case studies, GitHub Discussions link, FAQ link | CONFIRMED | `apicenter.commerce.naver.com/ko/basic/main` | 2026-07-11 | DIRECT-PROXY (r.jina.ai) | Retrieved real page content: 3 named solution case studies (revenue/MAU growth claims), "Core Resources"/"Support Channels" sections, FAQ covering "account creation without SmartStore registration," "marketplace sales procedures," "API usage costs" |
| Authentication method | Token issuance is OAuth2.0 `client_credentials`-shaped against `api.commerce.naver.com/external/v1/oauth2/token`, requiring `client_id`, `timestamp`, `client_secret_sign` | INFERRED | search synthesis citing `apicenter.commerce.naver.com` auth docs | 2026-07-11 | SEARCH-SYNTHESIS | No direct fetch of the specific `apicenter.commerce.naver.com` auth/getstart page succeeded this session; downgraded from the prior document's own `UNKNOWN` — still not directly confirmed, note upgraded evidence below |
| Authentication method (signature algorithm) | The non-standard signature (`client_secret_sign`) is generated by concatenating `client_id` + `timestamp` and hashing with **bcrypt** (`org.mindrot.jbcrypt.BCrypt` in the official Java example), then base64-encoding | CONFIRMED (official channel) | `github.com/commerce-api-naver/commerce-api/discussions/1862` | 2026-07-11 | DIRECT-GITHUB | Discussion #1862: a user debugging the official Java signature example imports `org.mindrot.jbcrypt.BCrypt`, `java.util.Base64`, `java.nio.charset.StandardCharsets`; the Naver maintainer directly assisted with this exact code, confirming bcrypt (not a generic hash) is the real algorithm used in official sample code. This **upgrades the prior document's `UNKNOWN` bcrypt claim to `CONFIRMED (official channel)`** — a materially important fact for any future signing-logic implementation, though the full request-parameter list is still not independently confirmed from the primary getstart page in this pass. |
| App/seller registration requirements | Only the store's "통합매니저" (Integrated Manager) can issue a Commerce API application | INFERRED | search synthesis of official-domain/GitHub content | 2026-07-11 | SEARCH-SYNTHESIS | Search summary states this directly, attributing it to GitHub Discussions/apicenter content, but the specific discussion thread carrying this exact statement was not independently opened and read verbatim this session — treat as plausible but unverified pending direct read |
| App/seller registration — IP allow-listing | "My Store Application" and "Solution Application" API access requires registering the calling device's IP address; unregistered-IP calls may be monitored/blocked (for solutions) | CONFIRMED (official channel) | `github.com/commerce-api-naver/commerce-api/discussions/2252` | 2026-07-11 | DIRECT-GITHUB | Maintainer quote: "API호출 IP는 실제 커머스API를 호출하는 기기의 IP를 설정하시면 됩니다"; distinguishes My Store Application IP-edit path (`커머스API센터 > 내 정보 > 내 스토어 애플리케이션 > ...`) from Solution Application path; states solution calls from unregistered IPs "차단될 수 있습니다" (may be blocked). **New confirmed fact vs. prior document**, which had this whole area as `UNKNOWN`. |
| App/seller registration — IP count limit | Up to 3 IPs may be registered per application | UNKNOWN | — | 2026-07-11 | — | Discussion #2252's actual fetched content explicitly does **not** state a number ("the discussion content does not specify how many IPs can be registered"); the "3 IPs" figure appeared only in a WebSearch synthesis of a different, unopened source — not carried forward as fact |
| Product registration API | A product-registration API exists; inputs discussed include `leafCategoryId` (resolved via a category-lookup API, not a parent category), `detailContent` submitted as an HTML string requiring quote-escaping, `visitAddressId`/`shippingAddressId`/`returnAddressId` resolved via address-book APIs, `SELF` vs. `SELLER` token types (only `SELF` works for individual sellers at time of the thread) | CONFIRMED (official channel) | `github.com/commerce-api-naver/commerce-api/discussions/246` | 2026-07-11 | DIRECT-GITHUB | Full Q&A read directly; matches and **exceeds** the prior document's synthesis-based summary (same discussion #246, now independently confirmed with more field-level detail: exact field names `leafCategoryId`, address-ID resolution mechanism, SELF/SELLER token distinction) |
| Product registration — catalog matching | Naver's product catalog can assign 2-30 different catalog IDs to items with identical brand+model; no API field distinguishes domestic/imported or active/deleted catalog status; origin/import info must be set manually via `originProduct.detailAttribute.originAreaInfo` | CONFIRMED (official channel) | `github.com/commerce-api-naver/commerce-api/discussions/2127` | 2026-07-11 | DIRECT-GITHUB | Official maintainer response read directly; not present at all in the prior document (new finding) |
| Product modification API | General channel-product modification API is `PUT /v2/products/channel-products/{channelProductNo}` (implied endpoint shape referenced directly in an official maintainer answer); modification requires submitting the **entire** current product payload — any field omitted from the request is treated as a deletion, not "leave unchanged" | CONFIRMED (official channel) | `github.com/commerce-api-naver/commerce-api/discussions/1903` | 2026-07-11 | DIRECT-GITHUB | Maintainer quote: "상품의 전체 데이터를 입력 후 재고 수량만 수정하신다면 정상 호출이 가능합니다"; explicit statement that omitted fields are deleted, not preserved. **This is a materially important, previously entirely-`UNKNOWN` fact** — see §1.5/options below and the cross-reference table |
| Options/Price/Stock API | **No dedicated stock-only (or price-only/option-only) update endpoint exists.** Price and stock changes go through the same general product-modification endpoint as every other field, requiring a full-payload re-submission (query current state first, then resubmit all fields with only the target field changed) | CONFIRMED (official channel) | `github.com/commerce-api-naver/commerce-api/discussions/1903`, `discussions/1669` | 2026-07-11 | DIRECT-GITHUB | Discussion #1903 (see above) is explicit and direct. Discussion #1669 (search-synthesis only, not independently opened this session, so treated as corroborating-but-secondary) separately describes sellers complaining there is "no API for batch updating product prices only," consistent with the same "no dedicated endpoint" conclusion. **This directly resolves the prior document's `UNKNOWN` at §1.5 — and resolves it in the opposite direction a reader might assume from Coupang's symmetric-sounding item-level APIs: Naver explicitly does NOT mirror Coupang's separate-endpoint design.** This is architecturally significant for the Phase 2 `SmartStoreAdapter` design proposed in the prior document (§3.2), which did not anticipate a full-payload-resubmission requirement. |
| Options/Price/Stock — combo/standard option stock | For combo-type or standard-type option products with stock management enabled, `originProduct.stockQuantity` must be **excluded** from the modification request (stock is auto-summed from per-option combination quantities) | CONFIRMED (official channel) | search synthesis citing `github.com/commerce-api-naver/commerce-api` content (not independently re-opened as a standalone thread this session) | 2026-07-11 | SEARCH-SYNTHESIS | Downgrade note: this specific sub-fact was returned inside a broader WebSearch synthesis of Naver Discussions content, not from a directly-opened, individually-cited thread in this session — tag as INFERRED, not CONFIRMED, pending a dedicated re-check |
| Image upload | A dedicated image-registration endpoint exists: `POST https://api.commerce.naver.com/external/v1/product-images/upload`; supports single-image upload from an external/partner CDN URL; accepted formats are **JPEG, GIF, PNG, BMP only**; returned URLs are then referenced when creating/modifying a product | CONFIRMED (official channel) | `github.com/commerce-api-naver/commerce-api/discussions/3212` | 2026-07-11 | DIRECT-GITHUB | Full Q&A read directly, including the exact endpoint URL, the exact error message ("올바른 이미지 파일이 아닙니다"), and 3 official-stated causes (unsupported format, MIME/Content-Type mismatch, extension mismatch). **This fully resolves the prior document's `UNKNOWN` at §1.6** — a materially important, previously-unconfirmed fact for any Phase 2A payload builder. |
| Notice information (고시정보) / category-required fields | A dedicated product-notice-information API exists; its `categoryId` parameter requires a **major/top-level ("대") category ID**, not a leaf category ID — submitting a leaf category ID silently returns no/wrong notice items; top-level category IDs are resolved via the same "전체 카테고리 조회" (`GET /v1/categories`) API | CONFIRMED (official channel) | `github.com/commerce-api-naver/commerce-api/discussions/1733` | 2026-07-11 | DIRECT-GITHUB | Full Q&A read directly: a seller's leaf-category ID (`50000471`, Cosmetics/Beauty > Base Makeup > CC Cream) was diagnosed by the maintainer as the root cause, with the fix being to use the parent major-category ID instead. **This fully resolves the prior document's `UNKNOWN` at §1.7** and is architecturally important for the Phase 2 category-metadata mapping proposed at §3.2 of the prior document (the mapping must resolve to the major-category level, not the leaf category actually used for product registration). |
| Order collection API | Endpoint confirmed as `POST /v1/pay-order/seller/product-orders/query` (or equivalent path fragment `/v1/pay-order/seller/product-orders/query` as directly quoted); `optionCode` is auto-assigned and not shown in SmartStore Center admin UI — `originProduct.detailAttribute.optionInfo.optionStandards.sellerManagerCode` should be used instead, and matches `data.productOrder.optionManageCode` in order-query responses | CONFIRMED (official channel) | `github.com/commerce-api-naver/commerce-api/discussions/1637` | 2026-07-11 | DIRECT-GITHUB | Full Q&A read directly, including the exact endpoint path fragment and exact field-name mapping. **This resolves the prior document's `UNKNOWN` exact-endpoint-name gap at §1.8.** |
| Dispatch/shipping/invoice API | "Modified Product Order History Search API" exists, queryable with `lastChangedType` (e.g. `DISPATCHED`); order retrieval requires precise timing alignment with the status-change window; official guidance for missing orders is to drop the `lastChangedType` filter and widen the date range instead | CONFIRMED (official channel) | `github.com/commerce-api-naver/commerce-api/discussions/1646` | 2026-07-11 | DIRECT-GITHUB | Full thread read directly; confirms and adds detail beyond the prior document's synthesis (root cause in the cited case was a multi-store/mismatched-application-ID issue, not an API defect) |
| Cancel/exchange/return API | `GetChangedProductOrderList` API's `lastChangedType` field includes a `COLLECT_DONE` status that applies **only to exchange claims, not returns**; `COLLECT_DONE` requires either manual Smart Store Center completion, courier tracking confirmation, or a successful Exchange Collection Complete API call; a "hold" setting changes which status value is returned | CONFIRMED (official channel) | `github.com/commerce-api-naver/commerce-api/discussions/1431` | 2026-07-11 | DIRECT-GITHUB | Full thread read directly. **Downgrade note vs. prior document**: the prior document's §1.10 summary ("supports return approval, return hold, return-hold release, return rejection, exchange pickup-completion, exchange re-shipment, exchange hold, exchange-hold release, exchange rejection") is a **broader claim than what this specific thread (#1431) actually contains** — the actual thread is narrowly about `COLLECT_DONE` semantics for exchanges vs. returns, not an enumeration of all nine listed actions. The nine-action list may be accurate (a fuller cancel/exchange/return API surface plausibly exists) but this session did not independently verify it against primary content — see cross-reference table, item downgraded to `INFERRED` for the parts not directly re-confirmed. |
| Rate limits / error handling | Exceeding the limit returns HTTP 429 with `{"code": "GW.RATE_LIMIT", "message": "요청이 많아 서비스를 일시적으로 사용할 수 없습니다."}`; a **token bucket algorithm** is used; **3 tiers** of limits exist — platform-wide API capacity, per-application-per-API RPS, and per-seller-token quota (commerce solutions/agencies only); `GNCP-GW-RateLimit-*`/`GNCP-GW-Quota-*` headers expose live quota state; HTTP 429 can also occur during service congestion even with remaining quota, since 2024-04-01; recommended retry is a 3-5 second delay | CONFIRMED (official channel) | `github.com/commerce-api-naver/commerce-api/discussions/6, 1538, 1648` | 2026-07-11 | DIRECT-GITHUB | All 3 threads read directly and cross-consistent. **New specific numeric fact vs. prior document (which had the exact numeric limit as `UNKNOWN`)**: "내 스토어 애플리케이션" (My Store Application, the individual-seller tier) is rate-limited to a **fixed 2 RPS**, with a one-time, non-consecutive "burst" mechanism that can borrow the next second's allowance. This resolves part of the prior document's `UNKNOWN` at §1.11 for at least this one tier. |
| Approval/review conditions | No official-domain source found describing a formal review SLA or explicit approval criteria | UNKNOWN | — | 2026-07-11 | — | No new evidence found this session; remains `UNKNOWN`, consistent with the prior document |
| Sandbox/test environment | No official-domain confirmation of a sandbox/testbed environment for Naver Commerce API was obtained | UNKNOWN | — | 2026-07-11 | BLOCKED-NO-METHOD-WORKED | Direct fetch of the relevant `apicenter.commerce.naver.com` pages was not achieved this session (proxy fetch reached only the main/portal page, not a specific sandbox-topic page); remains `UNKNOWN`, consistent with the prior document. This is still a material Phase 2B planning gap. |

---

## 2. Coupang Open API — Evidence Matrix

| Area | Claim | Status | Source URL | Verification date | Method | Evidence |
|---|---|---|---|---|---|---|
| Official API existence | Official developer portal at `developers.coupangcorp.com`; hosts categorized docs (Product API, Return API, order/shipment APIs, auth guides, notices) | CONFIRMED | `developers.coupangcorp.com/hc/en-us/articles/360033917473-Coupang-OPEN-API` | 2026-07-11 | DIRECT-PROXY (r.jina.ai) | Retrieved actual page text: "information to help developers to conduct OPEN API integration, including product registration guide, workflow, Hmac guide and FAQ" |
| App/seller registration requirements | To use Coupang Open API, a seller signs up in **Wing** (`wing.coupang.com`) and issues an Open API key there — **no separate "apply for API access" step exists** beyond having a Wing account; any Coupang seller can use the Open API | CONFIRMED | `developers.coupangcorp.com/hc/en-us/articles/360033917473-Coupang-OPEN-API` | 2026-07-11 | DIRECT-PROXY (r.jina.ai) | Direct quote retrieved: "There is no process required for using OPEN API - just issue an OPEN API Key in Wing." This **upgrades** the prior document's `CONFIRMED (existence only)` tag to full `CONFIRMED` with the actual quoted sentence, and specifically confirms the "no additional approval process" detail the prior document could only infer. |
| App/seller registration — business registration number | A business registration number is required to sign up as a Wing seller in Korea | INFERRED | search synthesis (same article family) | 2026-07-11 | SEARCH-SYNTHESIS | Not independently re-confirmed by direct fetch this session (the successfully proxy-fetched version of this article did not include this specific sentence in the retrieved excerpt); carried forward at a downgraded tier pending re-check |
| App/seller registration — key eligibility gates | Claimed: new sellers must deliver 3+ orders before Open API key eligibility; key not issuable during "store-opening" status | UNKNOWN | — | 2026-07-11 | — | Not found in any directly-fetched content this session either; remains `UNKNOWN`, same as prior document |
| Authentication method (HMAC) | HMAC (RFC2104-family keyed-hash construction, article states "RFC2014" which is itself likely a typo in the source for RFC 2104) using Access Key + Secret Key; signature message = `datetime + method + path + query string`, concatenated in that order; **algorithm is HmacSHA256**; datetime format is **`yyMMddTHHmmssZ` (UTC)**; Authorization header format is `CEA algorithm=HmacSHA256, access-key={accessKey}, signed-date={datetime}, signature={signature}`; official sample code provided for Java/Node.js/PHP/C#/Python/Classic ASP | CONFIRMED | `developers.coupangcorp.com/hc/en-us/articles/360033461914-Creating-HMAC-Signature` | 2026-07-11 | DIRECT-PROXY (r.jina.ai) | Full technical page content retrieved directly, including the literal Authorization header template and datetime format string. **This is a significant upgrade over the prior document**, which only had a search-synthesized paraphrase without the exact header format or datetime format string. **Downgrade note**: the prior document's specific claim of a **"5-minute" signature validity window** was NOT present in this directly-fetched page content ("the documentation does not specify a signature validity window or expiration timeframe" on this specific article) — downgrading that one sub-claim to `UNKNOWN`/not-reconfirmed; it may be documented on a different official page not fetched this session. |
| Authentication — key/integrator limits | Only one Open API key can be issued per seller ID (company code); max 2 "Integrators" per seller | CONFIRMED (existence only) | `developers.coupangcorp.com/hc/en-us/articles/20405856965145-What-is-an-OpenAPI-key` | 2026-07-11 | DIRECT-PROXY (r.jina.ai), partial | Retrieved page confirms an OpenAPI key is "provided as an access key and a secret key" and is used to authenticate + identify the seller (`vendorId`), and separately references a "Re-issue" concept — but the fetched excerpt did **not** explicitly state the "1 key / max 2 integrators" numbers verbatim. Downgraded from the prior document's flat `CONFIRMED (existence only)` tag to a partial confirmation: the key/secret-pair mechanism is confirmed; the specific numeric limits are not independently re-confirmed this session and should be treated as `INFERRED` pending a direct re-check of the full article (only an excerpt was returned). |
| Product registration API | **Endpoint: `POST /v2/providers/seller_api/apis/api/v1/marketplace/seller-products`.** Required fields include `displayCategoryCode`, `sellerProductName` (max 100 chars), `vendorId`, `saleStartedAt`/`saleEndedAt`, `deliveryMethod` (enum: SEQUENCIAL/COLD_FRESH/MAKE_ORDER/AGENT_BUY/VENDOR_DIRECT), `deliveryCompanyCode`, `deliveryChargeType` (enum: FREE/NOT_FREE/CHARGE_RECEIVED/CONDITIONAL_FREE), `deliveryCharge`, `freeShipOverAmount`, `deliveryChargeOnReturn`, `returnCenterCode`, return-location fields, `returnCharge` (must be 100-150% of delivery charge), `outboundShippingPlaceCode`, `vendorUserId`, `items` (max 200 options/product), `images` (>=1 REPRESENTATION image); `requested` boolean (`false`=temp-save only, `true`=auto-submit for approval) | CONFIRMED | `developers.coupangcorp.com/hc/en-us/articles/360033877853-Product-Creation` | 2026-07-11 | DIRECT-PROXY (r.jina.ai) | Full field-level schema retrieved directly. **This is a major upgrade over the prior document**, which had only `CONFIRMED (existence only)` with "exact field-level payload schema: UNKNOWN." This is now materially field-complete for a Phase 2A dry-run payload builder. |
| Product modification API | Before-approval modification allowed for most fields via the general Modify Product API. Post-approval: sale price and inventory can **only** be changed via dedicated item-level APIs, not this endpoint; `displayCategoryCode` cannot be changed after registration; `bundleInfo` cannot be changed (must re-register as a new product); an option once "approved" cannot be deleted; `offerCondition` is permanent | CONFIRMED | `developers.coupangcorp.com/hc/en-us/articles/360034156073-Modify-Product` | 2026-07-11 | DIRECT-PROXY (r.jina.ai) | Full page content retrieved, including direct quotes: "Modifying selling price via Product Modification API is only possible before request for product approval has been made" |
| Product creation/modification — Rocket Growth interaction | A distinct combined modification flow exists for sellers running Rocket Growth alongside Marketplace listings on the same product | CONFIRMED (existence only) | search synthesis, not re-opened directly this session | 2026-07-11 | SEARCH-SYNTHESIS | Not independently re-verified by direct fetch this session; carried forward unchanged from the prior document's tier (no new evidence either confirming or contradicting it) |
| Image upload / requirements | No official-domain source with specific image requirements (dimensions/format/count/background) was retrieved | UNKNOWN | — | 2026-07-11 | — | Not found in any directly-fetched content this session (the Product Creation article confirms only that >=1 `REPRESENTATION`-type image is required, not format/dimension constraints); remains materially `UNKNOWN`, consistent with the prior document |
| Options/Price API | **Dedicated endpoint: `PUT /v2/providers/seller_api/apis/api/v1/marketplace/vendor-items/{vendorItemId}/prices/{price}`.** Price must be in 10-won increments; default guard rail restricts changes to -50%/+100% of current price unless `forceSalePriceUpdate=true` is set; optional `apMinSalePrice`/`apActive` params control auto-pricing; success response is `{"code":"SUCCESS","message":"Completed price change.","data":null}` | CONFIRMED | `developers.coupangcorp.com/hc/en-us/articles/360034156273-Changing-price-of-each-item-of-a-product` | 2026-07-11 | DIRECT-PROXY (r.jina.ai) | Full page content retrieved directly, including the exact endpoint path and the exact success-response shape. **Major upgrade over the prior document**, which only listed the article's existence/title. |
| Stock/Inventory API | A distinct, separate quantity-change endpoint exists (title confirmed: "상품 아이템별 수량 변경" / item-level quantity change), separate from the price-change endpoint above and from the general product-modify endpoint | CONFIRMED (existence only) | search-confirmed official article title; not independently opened this session (effort was prioritized on price/invoice/cancel articles) | 2026-07-11 | SEARCH-SYNTHESIS (title only) | Endpoint existence is corroborated by the same "post-approval must use item-level API" quote confirmed for price (Modify-Product article, directly fetched — see above), which explicitly groups price and inventory together under the same constraint. The dedicated quantity endpoint's own field-level schema was not independently fetched this session — downgrade to `INFERRED` for schema-level detail, while the "separate from general modify" architectural fact is `CONFIRMED` via the Modify-Product article's own text |
| Notice information (고시정보) / category-required fields | **Category Metadata Query API** exists, taking `displayCategoryCode` as a required path parameter, returning: product attributes (type/dataType/INPUT-or-SELECT/required-optional/purchase-option-vs-search-option), product notice fields (required/optional per category), required documents (incl. parallel-import-specific conditions), certifications (mandatory/recommended/optional, with data-format requirements), and allowed product conditions (New/Refurbished/Used-Best/Good/Normal) | CONFIRMED | `developers.coupangcorp.com/hc/en-us/articles/360034035713-Category-Metadata-Query` | 2026-07-11 | DIRECT-PROXY (r.jina.ai) | Full page content retrieved directly. **Major upgrade over the prior document's `CONFIRMED (existence only)` tag** — the response schema is now field-complete for Phase 2A category-metadata mapping. |
| Notice information — mandatory purchase-option policy | A mandatory required-purchase-option-entry policy, effective 2026-02-02, with escalating enforcement | CONFIRMED (existence only) | search synthesis, not independently re-opened this session | 2026-07-11 | SEARCH-SYNTHESIS | Carried forward unchanged from the prior document's tier; not contradicted, not independently re-confirmed by direct fetch this session — this is explicitly a live/moving policy per the prior document and should be re-verified close to any actual implementation regardless of this document's tier |
| Order collection API | An "Order/Shipment" (발주서, Purchase Order/"PO") API family exists, distinct from the Product API. **Endpoint (PO list query by minute): `GET /v2/providers/openapi/apis/api/v5/vendors/{vendorId}/ordersheets`**, requiring `createdAtFrom`/`createdAtTo` (max 24-hour window), `status` (enum: ACCEPT/INSTRUCT/DEPARTURE/DELIVERING/FINAL_DELIVERY/NONE_TRACKING), optional `searchType=timeFrame`; response includes `shipmentBoxId`/`orderId`, timestamps, receiver/delivery detail, item pricing/discount, courier/tracking info, and a `nextToken` pagination cursor. Sibling endpoints exist for day-paged query and single-order query by `orderId`/`shipmentBoxId`. Korea/Taiwan markets only. | CONFIRMED | `developers.coupangcorp.com/hc/en-us/articles/360033792774-PO-list-query-by-Minute` | 2026-07-11 | DIRECT-PROXY (r.jina.ai) | Full page content retrieved directly, including the exact endpoint path and full response-field list. **This fully resolves the prior document's `UNKNOWN` exact-endpoint-name gap at §2.8** — a significant upgrade. |
| Dispatch/purchase-order (발주) API | Confirmed as the same "PO" (발주서) API family as Order Collection above — a 발주서 (purchase/dispatch order document) precedes fulfillment; sibling articles ("PO list query by day (paging)", "Single PO query using orderId", "Single PO query using shipmentBoxId") exist per official article titles | CONFIRMED (existence only) | `developers.coupangcorp.com/hc/en-us/articles/360033792774-PO-list-query-by-Minute` (family), sibling titles via search | 2026-07-11 | DIRECT-PROXY (r.jina.ai) for the minute-query article; SEARCH-SYNTHESIS (title only) for the 3 sibling articles | The core PO article is fully confirmed (see above); the 3 sibling articles were not independently opened this session — their existence is corroborated only by official-domain article titles returned directly in search results, which is stronger than a generic synthesis (an exact, specific, named official URL) but still short of a direct read, so tagged `INFERRED` for their specific field-level content while their existence is `CONFIRMED (existence only)` via title |
| Shipping/invoice API | Invoice-upload endpoint requires `vendorId`, `orderId`, `shipmentBoxId`, `deliveryCompanyCode`, `invoiceNumber` (blank string allowed for split shipping), `vendorItemId`, `splitShipping`, `preSplitShipped`, `estimatedShippingDate` (split-shipping only); order must be in "상품준비중" (Product in Preparation) status; successful upload transitions the order to "Shipped"/dispatch-instructed status; **duplicate invoice number within 6 months** triggers error code **`NOT_ALLOW_INVOICE_NUMBER_RE_UPLOAD`**, unless the recipient name+address match the prior use | CONFIRMED | `developers.coupangcorp.com/hc/en-us/articles/360033793014-송장업로드-처리` | 2026-07-11 | DIRECT-PROXY (r.jina.ai) | Full page content retrieved directly, including the exact error code name (`NOT_ALLOW_INVOICE_NUMBER_RE_UPLOAD`) and the same-recipient exception — both new, specific facts not in the prior document, which only had the 6-month window itself confirmed |
| Cancellation API | **Endpoint: `POST` (path parameters `vendorId`, `orderId`) for order cancellation.** Body requires `orderId`, `vendorItemIds[]`, `receiptCounts[]` (>0), `bigCancelCode` (fixed `"CANERR"`), `middleCancelCode` (enum: `CCTTER` inventory issue / `CCPNER` address issue / `CCPRER` pricing discrepancy), `userId`, `vendorId`. Response: `code` (200=full/partial success, 400=failed), `data.receiptMap` (per-receipt detail incl. `receiptType`: CANCEL or STOP_SHIPMENT), `data.failedVendorItemIds` (explicit per-item partial-failure reporting) | CONFIRMED | `developers.coupangcorp.com/hc/en-us/articles/360033843154-Cancel-an-order` | 2026-07-11 | DIRECT-PROXY (r.jina.ai) | Full request/response schema retrieved directly, including the exact fixed-value cancel-reason codes. **Major upgrade over the prior document**, which only had the general response shape (receipt ID/type/vendor item IDs/failed item IDs) without the request-side schema or the fixed code values. |
| Exchange API | No exchange-specific official article was independently opened this session (effort budget prioritized cancel/return/invoice); the only exchange-specific detail found remains the Naver-side `COLLECT_DONE` distinction (§1, not Coupang) | UNKNOWN | — | 2026-07-11 | — | Unchanged from the prior document's `UNKNOWN` tag; the prior document's caution that exchange specifics were only sourced from an unofficial third-party GitHub wrapper (`kyungdongseo/coupang`) still stands and was not re-investigated this session |
| Return API | **Endpoint: `GET /v2/providers/openapi/apis/api/v6/vendors/{vendorId}/returnRequests`.** Required: `vendorId`, `searchType=timeFrame` (for minute-level queries), `createdAtFrom`/`createdAtTo`. Optional: `status` (enum: RU=suspend shipment / UC=return received / CC=completed / PR=verification), `cancelType` (RETURN default / CANCEL), `maxPerPage` (default 50). Max query window: **31 days** ("could result in timeout error" if too much data). Korea/Taiwan markets only. A distinct `Return API` documentation section exists, confirming returns are first-class and separately documented. | CONFIRMED | `developers.coupangcorp.com/hc/en-us/articles/360033919613-Return-Cancellation-Request-List-Query` | 2026-07-11 | DIRECT-PROXY (r.jina.ai) | Full page content retrieved directly, including the exact endpoint path, exact status-code enum, and exact max-window figure. **Downgrade note vs. prior document**: the prior document stated the date-range query variant is documented with "a maximum 7-day window" — the actual directly-fetched content of this article states **31 days**, not 7 days. This is a real, material correction, not a restatement — flagged explicitly in the cross-reference table. |
| Rate limits / error handling | Default threshold: **more than 5 API calls per second per `vendorId`** triggers HTTP 429; recovery is described as being possible "within minutes to tens of minutes" after the overage stops; the policy is stated to have begun "on July 10" (year not stated in the retrieved excerpt — the prior document separately cited an official article dated 2023-10-12 for a strengthening of this policy specifically for product-related endpoints, which was not re-independently re-opened this session, so the "July 10" origin date and the "2023-10-12 strengthening" date are not confirmed to refer to the same event) | CONFIRMED | `developers.coupangcorp.com/hc/en-us/articles/20414599556889-Introduction-of-Open-API-rate-limit-policy` | 2026-07-11 | DIRECT-PROXY (r.jina.ai) | Full page content retrieved directly, including the direct quote "The API call limit applies by default to vendor IDs (vendorId) that make more than 5 API calls per second," confirming the prior document's `5 calls/sec/vendorId` figure at full `CONFIRMED` tier (previously it was `CONFIRMED` via search-title synthesis only) |
| Approval/restriction conditions | Product listing approval is a distinct workflow step from API key issuance; each product passes through create -> approval-request -> approval-completed | CONFIRMED (existence only) | `developers.coupangcorp.com/hc/en-us/articles/360033877853-Product-Creation` | 2026-07-11 | DIRECT-PROXY (r.jina.ai) | Directly confirmed as part of the Product Creation article's `requested` boolean semantics (see Product Registration row above); no formal approval-SLA/turnaround figure found, `UNKNOWN` for that sub-part, unchanged from prior document |
| Sandbox/test environment | Claimed: "There is no separate test environment for API" (testing must occur against production); a Postman-style testing workflow (choose environment from a combo box, set Body/raw/JSON, check URI/Method/Header/Parameter from the portal, click Send) is documented on a general "OPEN API Test Guide" article | INFERRED | `developers.coupangcorp.com/hc/en-us/articles/900001711363-Is-a-test-account-or-test-environment-provided-for-API` (title match) and `.../360033988873-OPEN-API-Test-Guide` (title match) | 2026-07-11 | SEARCH-SYNTHESIS (both articles); two separate `r.jina.ai` proxy fetch attempts against the FAQ article both returned an unrelated cached page (a 2021 incident notice), not the actual article content | **Explicitly not upgraded to CONFIRMED** despite a very specific, exact-title WebSearch synthesis stating "There is no separate test environment for API" — per the task's evidentiary rule, this remains a search-synthesis-sourced claim, not a directly-read one, because both attempts to actually fetch the article's real content failed to return matching content. This is a **materially important gap for Phase 2B** and should be the first fact a human re-verifies with authenticated browser access, since it directly determines whether Phase 2B (§4 of the prior document) can mean an isolated sandbox or must mean a real, low-risk production account. |

---

## 3. Claim ↔ Prior Document Cross-Reference Table

For every claim in `docs/COMMERCE_PHASE_2_UPLOAD_ARCHITECTURE.md` tagged `CONFIRMED`,
`CONFIRMED (existence only)`, or `CONFIRMED (official channel)`, this table states whether this
session's re-verification confirms it at the same tier, upgrades it, downgrades it, or could not
be checked.

| # | Prior document claim (section) | Prior tier | Re-verification result | New tier | Reason |
|---|---|---|---|---|---|
| 1 | Naver Commerce API Center + official GitHub org exist (§1.1) | CONFIRMED | Re-confirmed directly | CONFIRMED (same) | GitHub org fetched directly again; portal main page also newly reached via proxy |
| 2 | Naver API covers product mgmt/order/inventory/shipping as one surface (§1.1) | CONFIRMED (existence only) | Not independently re-checked this session as a standalone claim | CONFIRMED (existence only) (unchanged) | Consistent with, but not separately re-derived from, this session's per-area findings (§1.4-1.9 above collectively support it) |
| 3 | Seller/app registration requires 통합매니저 role (§1.2) | UNKNOWN (third-party only) | Still not directly confirmed by primary-source read this session | INFERRED | Search synthesis of GitHub/official content states it directly, but the specific carrying thread was not opened verbatim — upgraded from "third-party only" (prior tier) to "official-source-attributed but not directly read" (this session), net effect is a **partial upgrade in confidence, not to CONFIRMED** |
| 4 | Naver auth = OAuth2.0 client_credentials + client_secret_sign (bcrypt) signature (§1.3) | UNKNOWN | **Partially confirmed**: the bcrypt algorithm specifically is now directly confirmed via official GitHub code-support thread | **SPLIT**: bcrypt mechanism = CONFIRMED (official channel); full OAuth2/endpoint/parameter claim = still UNKNOWN | Discussion #1862 directly confirms bcrypt via official maintainer-assisted code; the broader OAuth2 token-endpoint/parameter-set claim was not independently re-verified against the primary getstart page this session (still blocked) |
| 5 | Product registration API exists; fields incl. name/price/stock/category, HTML detail content (§1.4) | CONFIRMED (official channel) | Re-confirmed directly, with more detail | CONFIRMED (official channel), strengthened | Same discussion #246 re-opened and independently read; matches prior summary and adds detail (leafCategoryId resolution, address-ID mechanism, SELF/SELLER tokens) |
| 6 | Option/price/stock existence confirmed; separate-endpoint structure UNKNOWN (§1.5) | CONFIRMED (existence only) / UNKNOWN (separate-endpoint question) | **The separate-endpoint question is now answered**, and answered "no" | CONFIRMED (official channel): **no dedicated endpoint exists** | Discussion #1903 directly and explicitly states the general product-modify endpoint (full-payload resubmission) is the only path — this resolves, rather than merely re-confirms, the prior `UNKNOWN` |
| 7 | Image upload mechanism UNKNOWN (§1.6) | UNKNOWN | **Now resolved**: dedicated `product-images/upload` endpoint confirmed | CONFIRMED (official channel) | Discussion #3212 directly confirms the endpoint, accepted formats, and failure modes |
| 8 | Notice info/category-required fields UNKNOWN (§1.7) | UNKNOWN | **Now resolved**: notice API requires major-category (not leaf-category) IDs | CONFIRMED (official channel) | Discussion #1733 directly confirms via a diagnosed real support case |
| 9 | Order collection API exists, exact endpoint UNKNOWN (§1.8) | CONFIRMED (existence only) | Exact endpoint now confirmed | CONFIRMED (official channel), strengthened | Discussion #1637 directly states the endpoint path fragment |
| 10 | Dispatch/invoice API via lastChangedType=DISPATCHED (§1.9) | CONFIRMED (official channel) | Re-confirmed directly | CONFIRMED (official channel) (same) | Same discussion #1646 re-opened; content matches |
| 11 | Cancel/exchange/return API supports 9 named actions (return approval/hold/hold-release/rejection, exchange pickup-complete/re-ship/hold/hold-release/rejection) (§1.10) | CONFIRMED (official channel) | **Downgraded** — the cited thread (#1431), read directly, only supports the narrower `COLLECT_DONE`/exchange-vs-return distinction, not the full 9-action enumeration | INFERRED (for the 9-action list); CONFIRMED (official channel) for the narrower COLLECT_DONE fact actually found in #1431 | The prior document's synthesis appears to have generalized beyond what discussion #1431 itself contains; the 9-action list was not independently re-derived from any other directly-read source this session |
| 12 | Rate limit: GW.RATE_LIMIT/429/token bucket/GNCP headers (§1.11) | CONFIRMED (official channel) | Re-confirmed directly, plus a new specific number found | CONFIRMED (official channel), strengthened | 3 discussions re-opened directly; new fact: My Store Application tier = fixed 2 RPS |
| 13 | Approval/review conditions UNKNOWN (§1.12) | UNKNOWN | No new evidence found | UNKNOWN (unchanged) | — |
| 14 | Sandbox/test environment UNKNOWN (§1.13) | UNKNOWN | No new evidence found (proxy fetch reached only the main portal page) | UNKNOWN (unchanged) | — |
| 15 | Coupang official Open API portal exists (§2.1) | CONFIRMED | Re-confirmed via direct proxy fetch (not just search title) | CONFIRMED, strengthened (was search-title-only before) | — |
| 16 | Wing seller account + business reg. number required (§2.2) | CONFIRMED (existence only) | Wing requirement re-confirmed directly with exact quote; business-reg.-number sub-claim not re-confirmed | SPLIT: Wing requirement = CONFIRMED (upgraded); business reg. number = INFERRED (downgraded from existence-only, pending re-check) | Direct proxy fetch retrieved the Wing/no-extra-process quote verbatim but the excerpt did not include the business-registration-number sentence |
| 17 | HMAC auth: Access Key + Secret Key, timestamp+method+path+query, ~5-minute validity, sample code in 6 languages (§2.3) | CONFIRMED | Mechanism, algorithm, header format, message-construction order all re-confirmed and detailed further; **5-minute validity window specifically NOT found in the fetched content** | SPLIT: mechanism/algorithm/header format = CONFIRMED, strengthened; 5-minute validity = downgraded to UNKNOWN (not reconfirmed) | Direct proxy fetch of the actual HMAC article; validity-window sentence simply was not present in what was retrieved — may exist elsewhere on the page or a related page not captured |
| 18 | 1 key/seller, max 2 integrators (§2.3) | CONFIRMED (existence only) | Key/secret pair mechanism confirmed; specific 1-key/2-integrator numbers not present in the fetched excerpt | INFERRED (downgraded) | Partial-content fetch; numbers not independently reconfirmed |
| 19 | Product creation/modification API, temp-save->approval flow, `requested` param (§2.4) | CONFIRMED (existence only), field schema UNKNOWN | **Field schema now fully confirmed** | CONFIRMED, strengthened (schema resolved) | Direct proxy fetch of both Product Creation and Modify Product articles, full field lists retrieved |
| 20 | Rocket Growth combined modification flow (§2.4) | CONFIRMED (existence only) | Not independently re-checked | CONFIRMED (existence only) (unchanged) | Not re-verified either way this session |
| 21 | Dedicated item-level price/stock endpoints, post-approval must use them, vendorItemId issued only post-approval (§2.5) | CONFIRMED / CONFIRMED (existence only) | Price endpoint fully confirmed with schema; post-approval constraint fully confirmed; stock endpoint existence corroborated but its own schema not independently fetched; vendorItemId-timing claim not independently re-checked this session | Price endpoint = CONFIRMED, strengthened; post-approval constraint = CONFIRMED, strengthened; stock endpoint schema = INFERRED; vendorItemId timing = CONFIRMED (existence only) (unchanged, not re-checked) | — |
| 22 | Image requirements UNKNOWN (§2.6) | UNKNOWN | No new evidence found (only ">=1 REPRESENTATION image" confirmed, not dimensions/format) | UNKNOWN (unchanged, minor detail added) | — |
| 23 | Category Metadata Query API for notice/option/cert requirements (§2.7) | CONFIRMED (existence only) | **Full response schema now confirmed** | CONFIRMED, strengthened | Direct proxy fetch, full field list retrieved |
| 24 | Mandatory purchase-option policy effective 2026-02-02 (§2.7) | CONFIRMED (existence only) | Not independently re-checked | CONFIRMED (existence only) (unchanged) | Still a live/moving policy per the prior document's own caveat; re-verify close to implementation regardless |
| 25 | Order API category exists, schema UNKNOWN (§2.8) | CONFIRMED (existence only), schema UNKNOWN | **Schema now fully confirmed** | CONFIRMED, strengthened (schema resolved) | Direct proxy fetch of the PO-list-by-minute article, full endpoint + field list retrieved |
| 26 | Invoice upload API, dispatch-instructed state, 6-month duplicate window (§2.9) | CONFIRMED | Re-confirmed directly, plus new detail (exact error code, same-recipient exception) | CONFIRMED, strengthened | Direct proxy fetch |
| 27 | Purchase-order/outbound-slip step precedes invoice, exact endpoints UNKNOWN (§2.9) | CONFIRMED (existence only), endpoints UNKNOWN | PO endpoint family now confirmed (see #25); outbound-slip (출고전표) step specifically not independently re-checked | CONFIRMED (existence only) for PO family (strengthened); outbound-slip step = CONFIRMED (existence only) (unchanged) | — |
| 28 | Return/cancel API: list query, cancel-order, withdrawal history, **7-day window** (§2.10) | CONFIRMED | Cancel-order and list-query APIs re-confirmed directly with full schema; **the 7-day window figure is contradicted** — the directly-fetched article states a 31-day max window | Cancel-order/list-query = CONFIRMED, strengthened; **window figure = CORRECTED from 7 days to 31 days** | Direct proxy fetch of `Return-Cancellation-Request-List-Query` explicitly states "up to 31 days" — this is a real, material correction, not a restatement of the prior figure. The prior document's "7-day window" may have referred to a different, still-unfetched sibling article (the date-range variant), so this is flagged as a correction pending a direct fetch of that specific sibling article, not a certainty that the prior figure was simply wrong. |
| 29 | Exchange-specific API endpoints UNKNOWN (§2.10) | UNKNOWN | No new evidence found | UNKNOWN (unchanged) | — |
| 30 | Rate limit: 429, ~5 calls/sec/vendorId default, strict blocking since 2023-10-12 (§2.11) | CONFIRMED | 5-calls/sec/vendorId figure re-confirmed directly with exact quote; the 2023-10-12 strengthening-specifically-for-product-APIs claim not independently re-checked this session | 5 calls/sec figure = CONFIRMED, strengthened (was title-only before); 2023-10-12 product-API-specific strengthening = CONFIRMED (existence only) (unchanged, not re-verified) | — |
| 31 | Approval SLA/turnaround UNKNOWN (§2.12) | UNKNOWN | No new evidence found | UNKNOWN (unchanged) | — |
| 32 | Neither platform's sandbox availability confirmed (Summary) | UNKNOWN (both) | Naver: still UNKNOWN. Coupang: a specific, exact-title official FAQ article ("Is a test account or test environment provided for API?") was found and its likely answer surfaced via search synthesis ("no separate test environment"), but **two direct-fetch attempts both failed to retrieve matching content** | Naver = UNKNOWN (unchanged); Coupang = INFERRED (a specific, plausible, but not directly-read answer — explicitly not CONFIRMED per task rule) | This is the single most operationally important unresolved gap for Phase 2B scoping in both platforms |

### 3.1 Summary counts

- Claims **re-confirmed at the same or a strengthened tier**: 16 (Naver: #1, #5, #9, #10, #12; Coupang: #15, #17 (mechanism only), #19, #21 (price+constraint only), #23, #25, #26, #27 (PO family only), #28 (cancel/list-query only), #30 (5-calls/sec only))
- Claims **newly resolved from UNKNOWN to CONFIRMED** this session: 4 (Naver §1.5 no-dedicated-endpoint, §1.6 image upload, §1.7 notice-info major-category rule, plus the bcrypt sub-fact of §1.3)
- Claims **downgraded** (tier lowered, evidence found insufficient on direct read): 6 (#3 partial, #11 the 9-action list, #16 business-reg-number sub-claim, #17 the 5-minute validity sub-claim, #18 the 1-key/2-integrator numbers, #21 stock-endpoint schema)
- Claims **corrected** (directly contradicted by primary content, not merely unconfirmed): 1 (#28 — 7-day window vs. the directly-fetched 31-day window; flagged as needing sibling-article re-check rather than a certain error)
- Claims **unchanged, no new evidence either way**: 9 (#2, #6 combo-stock sub-fact only, #13, #14, #20, #22, #24, #29, #31)
- Claims that remain the single biggest open risk for Phase 2B scoping: **sandbox/test-environment availability, both platforms** (#32)

---

## 4. Genuinely New Findings Not in the Prior Document

These were not present in `docs/COMMERCE_PHASE_2_UPLOAD_ARCHITECTURE.md` at all (not merely
strengthened — newly discovered facts):

1. **Naver has no dedicated price/stock/option update endpoint** — the general product-modification
   endpoint must be used, and it requires resubmitting the entire product payload (omitted fields
   are deleted, not preserved). This is architecturally significant and contradicts an implicit
   Coupang-style assumption the prior document's `SmartStoreAdapter` proposal (§3.2) did not
   anticipate.
2. **Naver's product-notice-information API requires a major/top-level category ID**, not the leaf
   category ID used for actual product registration — a mapping step the prior document's §3.2/§3.5
   proposal did not call out.
3. **Naver's dedicated image-upload endpoint**: `POST https://api.commerce.naver.com/external/v1/product-images/upload`, accepting only JPEG/GIF/PNG/BMP, with returned URLs referenced in the product create/modify payload.
4. **Naver's "My Store Application" rate-limit tier is a fixed 2 requests/second**, with a
   one-time, non-consecutive burst mechanism.
5. **Naver bcrypt-based signature algorithm** independently confirmed via an official
   maintainer-assisted Java code-support thread (`org.mindrot.jbcrypt.BCrypt`).
6. **Coupang's product-creation and item-level price-change endpoints' exact field-level schemas**
   (see §2 evidence matrix rows above) — the prior document had these as existence-only with schema
   explicitly `UNKNOWN`.
7. **Coupang's exact cancel-order request schema**, including the fixed `bigCancelCode="CANERR"`
   and the 3-value `middleCancelCode` enum (`CCTTER`/`CCPNER`/`CCPRER`).
8. **Coupang's exact invoice-duplicate error code**: `NOT_ALLOW_INVOICE_NUMBER_RE_UPLOAD`, plus the
   same-recipient exception to the 6-month duplicate rule.
9. **Coupang's return-request-list query window is 31 days**, not 7 days as the prior document
   stated — a direct correction pending confirmation this refers to the same API variant.
10. **Coupang's Order/PO API exact endpoint**: `GET /v2/providers/openapi/apis/api/v5/vendors/{vendorId}/ordersheets`.
11. **`r.jina.ai` as a working alternate access path** for both blocked domains — a genuinely new
    methodological finding for future research passes on this project, not a platform fact, but
    operationally important: it means future re-verification of the still-`UNKNOWN`/`INFERRED`
    items in this document (Naver auth token endpoint, both platforms' sandbox questions, Coupang
    business-registration requirement, Coupang key/integrator numeric limits, Coupang exchange API,
    Coupang image requirements) is plausibly achievable **without** a human needing authenticated
    browser access, contrary to the prior document's blanket assumption that only a human browser
    session could get past the blocks. This does not replace the `CTO GATE` human-verification
    requirement the prior document correctly establishes before any Phase 2 implementation — it
    only means a further automated research pass is likely to close more gaps before that human
    review happens.

## 5. Items Still Requiring Human Direct Verification (Unresolved `CTO GATE` Items)

Unchanged or only partially addressed by this session, in priority order for Phase 2 planning risk:

1. Sandbox/test environment availability for **both** platforms (Coupang: `INFERRED` "no sandbox";
   Naver: fully `UNKNOWN`) — highest priority, directly blocks Phase 2B scoping as defined in the
   prior document's §4 phased plan.
2. Naver's full OAuth2 token-request parameter set and endpoint (only the bcrypt signature
   sub-algorithm was confirmed this session, not the full token-issuance flow).
3. Naver's seller/app registration approval process and any 통합매니저-role requirement (still
   `INFERRED`, not directly read).
4. Coupang's exchange-specific API (still fully `UNKNOWN`).
5. Coupang's image requirements (dimensions/format/background — still fully `UNKNOWN`).
6. Coupang's 1-key/2-integrator and business-registration-number claims (downgraded to `INFERRED`
   this session pending a full, not-excerpted, re-fetch of the source articles).
7. The Coupang 7-day-vs-31-day return-query-window discrepancy (§3, item 28) — resolve by fetching
   the specific date-range-variant sibling article directly.
8. The full 9-action Naver cancel/exchange/return API surface claimed in the prior document's §1.10
   (only the narrower `COLLECT_DONE` sub-fact was directly re-confirmed this session).

---

## 6. Methodology Notes for Future Re-Verification Passes

- `r.jina.ai/<url>` reliably bypassed the Coupang 403 block in this session for **specific article
  URLs** obtained from `WebSearch` results (not guessed paths) — guessing plausible-looking Naver
  `apicenter.commerce.naver.com` sub-paths (e.g. `/ko/basic/getstart/before`) failed even through
  the proxy, returning a real "page not found" from the origin, suggesting the guessed path was
  simply wrong (not that the proxy failed) — future passes should always source exact URLs from
  `WebSearch` first, never guess a path pattern.
- The `r.jina.ai` proxy occasionally returned **stale/wrong cached content** for at least one
  specific Coupang article (`900001711363-Is-a-test-account-or-test-environment-provided-for-API`,
  tried twice, both times returning an unrelated 2021 incident-notice page). Any single proxy fetch
  should be treated as provisional until its content is checked for topical relevance to the
  requested URL — a mismatch is a signal to retry or fall back to a lower confidence tier, not to
  tag the fact `CONFIRMED` anyway.
- `web.archive.org` and Google-Translate-proxy (`translate.goog`) access paths were tried and both
  failed in this environment for these domains — not worth retrying without an environment change.
- `github.com/coupang` was checked and confirmed to have **no** Commerce/Open-API-relevant content —
  Coupang has no equivalent to Naver's official GitHub Discussions technical-support channel; future
  passes should not spend budget searching for Coupang GitHub-based facts.
